services:
  discarr:
    build: .
    container_name: discarr
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./config:/app/config
    env_file:
      - ./config/.env
    environment:
      - TZ=${TZ:-UTC}
    # Health handled by Dockerfile healthcheck
